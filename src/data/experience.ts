import type { Experience } from "@/types/experience"

// Add only verified professional history. The release content check intentionally
// fails while this collection is empty.
export const experienceEntries: Experience[] = []
